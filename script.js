/*fetch(`./data.json`).then(request => {
    return request.json()
}).then(data => {
    const displayList = (dataValue) => {
        const myList = document.getElementById(`list`)
        myList.innerHTML += `<li>
        <span>${dataValue.title}</span>
        </li>`
    }

    for(const dataValue of data) {
        displayList(dataValue)
    }
})*/




